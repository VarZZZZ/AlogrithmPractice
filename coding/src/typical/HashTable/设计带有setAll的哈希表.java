package typical.HashTable;

/**
 * @Author: ly
 * @Date: 2020/7/9 9:55
 * @Version 1.0
 */
public class 设计带有setAll的哈希表 {
    /**
     * 加入时间戳结构
     * 1.每一个记录都加上一个时间，表明记录是何时建立的。
     * 2.设置一个setAll记录时也加上时间
     * 3.查询时，如果记录时间小于setAll时间，则返回setAll记录，否则，返回原本记录
     */
}
